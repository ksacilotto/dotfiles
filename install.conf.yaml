- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.screenrc: screenrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
